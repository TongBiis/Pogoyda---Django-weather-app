services:
  redis:
    image: redis:alpine

  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"